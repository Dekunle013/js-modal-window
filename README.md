A reusable modal/popup component triggered by multiple buttons, with overlay and close functionality. Built with vanilla JavaScript and event delegation.

# Multiple trigger buttons using a loop

# Overlay and modal toggle with class manipulation

# Click-outside-to-close behavior

# Escape key event handler to close the modal
